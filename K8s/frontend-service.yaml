apiVersion: v1
kind: Service
metadata:
  name: jiradata-frontend-service
  namespace: default
  labels:
    app: jiradata-frontend
spec:
  type: LoadBalancer
  selector:
    app: jiradata-frontend
  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP

